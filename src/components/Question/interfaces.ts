export interface QuestionProps {
    quantitySelected: string
}

export interface QuestionFieldsProps {
    question: string
    firstOption: string
    secondOption: string
    thirdOption: string
    fourthOption: string
}